/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com'],
      },
    api: {
      bodyParser: true,
    }
  }


export default nextConfig;
